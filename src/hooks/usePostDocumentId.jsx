import {collection, getDocs, limit, query, where} from 'firebase/firestore'
import {useState, useEffect} from 'react'
import { db } from '../firebase';


const usePostDocumentId = () => {
    
}

export default usePostDocumentId